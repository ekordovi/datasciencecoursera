setwd("~/Desktop/R_files/datasciencecoursera")

# trying to calc mean from a bunch of pollution sensors 
pullutantmean <- function(directory, pollutant, id = 332) {
  for (i in id){
    read.csv("001.csv")
  }
  
}